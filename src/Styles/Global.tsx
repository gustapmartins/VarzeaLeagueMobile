import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
        marginTop: 40,  
    //     backgroundColor: themeContext?.theme == "Light" ? COLORS.primary : COLORS.success,
    },
})

export default globalStyles;